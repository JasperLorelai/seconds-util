name: 🧪 Run Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: ✅ Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v3

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📚 Install Dependencies
        run: npm install

      - name: 🧪 Run Tests
        run: npm test
